[tool.black]
line-length = 100
target-version = ["py311"]
include = 'mcp_cli/.*\.py'

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["mcp_cli"]
extend-exclude = ["bin", "tests", "scripts", "docs"]

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP"]
fixable = ["ALL"]

